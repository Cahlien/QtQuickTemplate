# Project-wide environment variable defaults (tracked in git).
# Developer-specific overrides go in .env.local (gitignored).

# Android — Google Play upload defaults
ANDROID_PLAY_TRACK=internal
ANDROID_PLAY_RELEASE_STATUS=completed
